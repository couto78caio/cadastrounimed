[tool.poetry]
name = "projeto_saude"
version = "0.1.0"
description = "Formulário de cadastro de plano de saúde com Flask"
authors = "Caio Couto"

[tool.poetry.dependencies]
python = "^3.9"  # Ou a versão do seu Python
flask = "^2.0.0"  # Ou a versão que o Poetry escolheu
pandas = "^1.3.0" # Ou a versão que o Poetry escolheu

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
